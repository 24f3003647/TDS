name: Simple Workflow

on:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: 24f3003647@ds.study.iitm.ac.in
        run: echo "Hello, world!"

      - name: Done
        run: echo "Workflow completed successfully!"
